# Engineering rules

Run formatting, tests, Clippy, compliance, and documentation checks before release. Preserve explicit failures and provenance; do not add downstream path dependencies. Changes follow Gates A through D and registered business rules.
